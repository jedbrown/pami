# begin_generated_IBM_copyright_prolog                             #
#                                                                  #
#  --------------------------------------------------------------- #
#                                                                  #
# (C) Copyright IBM Corp.  2009, 2009                              #
# IBM CPL License                                                  #
#                                                                  #
#  --------------------------------------------------------------- #
#                                                                  #
# end_generated_IBM_copyright_prolog                               #

VPATH		= @abs_srcdir@
SUBDIRS         = time client context p2p @XTRA_TEST_SUBDIRS@
NOTBUILT        = 
APPS            = xmihello.${EXTENSION} barrier.${EXTENSION} bcast.${EXTENSION} \
		bcast_advisor.${EXTENSION} allreduce.${EXTENSION} allgather.${EXTENSION} allgatherv.${EXTENSION} \
		scatter.${EXTENSION} scatterv.${EXTENSION} bcast_subcomm.${EXTENSION} \
		ambcast.${EXTENSION} alltoall.${EXTENSION} bcast_subcomm2.${EXTENSION} 

include @abs_top_builddir@/Build.rules

ifeq (@BUILDENV@,ade)
%.${EXTENSION}: %.c
	cp ${CRT1} .
	cp ${CRTI} .
	cp ${CRTN} .
	${CROSS_CXX} -I${INSTALL_DIR}/include -I@abs_srcdir@/../common/include ${CROSS_CXXFLAGS} $< -o $@ ${CROSS_LDFLAGS} -L${_BOSDEPS_USR_LIB_} @POELIB@ @IBMMPILIB@ 
#	${CROSS_CXX} -I${INSTALL_DIR}/include -I@abs_srcdir@/../common/include ${CROSS_CXXFLAGS} -c $< -o $@.o
	rm `basename ${CRT1}`
	rm `basename ${CRTI}`
	rm `basename ${CRTN}`
else

%.${EXTENSION}: %.c
	${CROSS_CXX} -I${INSTALL_DIR}/include ${CROSS_CXXFLAGS} $< -o $@ ${CROSS_LDFLAGS} -L../../${INSTALL_DIR}/lib
%.${EXTENSION}: %.cc
	${CROSS_CXX} -I${INSTALL_DIR}/include ${CROSS_CXXFLAGS} $< -o $@ ${CROSS_LDFLAGS} -L../../${INSTALL_DIR}/lib
%.${EXTENSION}: %.cpp
	${CROSS_CXX} -I${INSTALL_DIR}/include ${CROSS_CXXFLAGS} $< -o $@ ${CROSS_LDFLAGS} -L../../${INSTALL_DIR}/lib

endif

default: ${APPS}
